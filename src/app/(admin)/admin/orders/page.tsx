import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function AdminOrdersPage() {
    return (
        <AdminPlaceholder
            eyebrow="Orders"
            title="Order operations belong in admin."
            description="This page will become the home for order review and status updates. It separates operational tasks from the customer’s personal order history."
        />
    );
}
